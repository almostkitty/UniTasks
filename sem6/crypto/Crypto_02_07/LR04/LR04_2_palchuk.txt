> NULL;
                               0
;
> with(numtheory);
> with(RandomTools);
> hash := proc (x) options operator, arrow; `mod`(x, 5) end proc;
                    hash := x -> `mod`(x, 5)
;
> p1 := nextprime(Generate(integer(range = 2 .. 10^6)));
                          p1 := 113513
;
> p2 := nextprime(Generate(integer(range = 2 .. 10^6)));
                          p2 := 958213
;
> M := p1*p2;
                       M := 108769632269
;
> ifactor(M);
                       (113513) (958213)
;
> d := Generate(integer(range = 2 .. 10^4));
                           d := 8059
;
> igcd(d, phi(M));
                               1
;
> d*e = `mod`(1, phi(M));
                      754309967372641 = 1
;
> e := `mod`(`&^`(d, phi(phi(M))-1), phi(M));
                        e := 93598457299
;
> x := `mod`(Generate(integer(range = 10^4 .. 10^15)), M);
                        x := 16300367058
;
> s := `mod`(`&^`(hash(x), d), M);
                        s := 95734047032
;
> x1 := `mod`(`&^`(s, e), M);
                            x1 := 3
;
> hash(x)-x1;
                               0
;
> 
;
